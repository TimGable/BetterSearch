using System.Windows;

[assembly:ThemeInfo(
    // The app keeps its styles in application/window resources instead of theme dictionaries.
    ResourceDictionaryLocation.None,

    // If a generic WPF resource is added later, look for it in this assembly.
    ResourceDictionaryLocation.SourceAssembly
)]
